export function PresetSelect() {
  return (
    <div>
      {/* Prev arrow */}
      <button>◄</button>
      {/* Preset name pill */}
      <span>SOFT PAD</span>
      {/* Next arrow */}
      <button>►</button>
      {/* Category + number + dots */}
    </div>
  );
}
